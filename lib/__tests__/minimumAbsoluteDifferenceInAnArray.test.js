const assert = require('assert');
const minimumAbsoluteDifferenceInAnArray = require('../index.js');

describe('minimumAbsoluteDifferenceInAnArray', () => {
  it('has a test', () => {
    assert(false, 'minimumAbsoluteDifferenceInAnArray should have a test');
  });
});
